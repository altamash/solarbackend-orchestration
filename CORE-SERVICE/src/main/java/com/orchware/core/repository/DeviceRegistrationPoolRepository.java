package com.orchware.core.repository;

import com.orchware.core.model.DeviceRegistrationPool;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DeviceRegistrationPoolRepository extends JpaRepository<DeviceRegistrationPool, Long> {
}
