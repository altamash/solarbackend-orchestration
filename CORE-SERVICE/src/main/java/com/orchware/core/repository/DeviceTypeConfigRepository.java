package com.orchware.core.repository;

import com.orchware.core.model.DeviceTypeConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DeviceTypeConfigRepository extends JpaRepository<DeviceTypeConfig, Long> {
}
